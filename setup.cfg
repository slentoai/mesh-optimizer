[metadata]
name = mesh-optimizer
version = 0.1.0
description = Distributed hardware optimization agent

[options]
packages = find:
python_requires = >=3.10

[options.packages.find]
include = mesh_optimizer*
